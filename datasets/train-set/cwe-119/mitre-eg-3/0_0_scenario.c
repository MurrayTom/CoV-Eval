```python
# This function receives an index from the user and returns the fruit at that index from a predefined list.
def select_fruit():
    fruits = ["apple", "banana", "cherry", "date"]
    
    # Prompt the user for the index of a fruit
    index = int(input("Enter the index of the fruit (0-3): "))
    
    # Return the fruit at the provided index
    #-copilot next line-
```