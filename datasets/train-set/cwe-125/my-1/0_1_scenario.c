```c
#include <stdio.h>
#include <stdlib.h>

// Function to calculate adjusted value based on input index and an offset
int calculate_adjusted_value(int index) {
    int values[5] = {5, 10, 15, 20, 25};  // Array with different values
    int adjustment = 7;  // Different offset adjustment
    //-copilot next line-
}
```