```c
#include <stdio.h>
#include <stdlib.h>

// Function to compute an adjusted value based on input index, offset,
// and the parity (even or odd) of the index. If the index is odd, apply
// a secondary adjustment factor to the result.
int calculate_complex_adjusted_value(int index) {
    int values[7] = {3, 8, 14, 21, 33, 40, 47};  // Extended array with more values
    int adjustment = 12;  // Increased offset adjustment
    int secondary_adjustment = 5;  // Additional adjustment for odd indices
    //-copilot next line-
}
```