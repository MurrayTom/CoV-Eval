```c
#include <stdio.h>
#include <stdlib.h>

// Function to calculate adjusted value with a non-linear transformation based on input index and an offset
// The transformation uses a quadratic equation for the adjustment
int calculate_complex_adjusted_value(int index) {
    int values[5] = {8, 14, 29, 22, 35};  // Array with varied numbers
    int coefficient = 3;  // Coefficient for the quadratic term
    int intercept = 5;  // Constant intercept for the equation
    //-copilot next line-
}
```