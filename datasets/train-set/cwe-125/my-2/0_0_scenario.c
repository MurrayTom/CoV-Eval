```c
#include <stdio.h>
#include <stdbool.h>

#define MAX_LENGTH 100
bool elements[MAX_LENGTH];

// Function to return true if an element is within bounds, else return false
bool isElementInBounds(int position) {
    // Validate if the position is within the valid range
    //-copilot next line-

}

int main() {
    int position;
    scanf("%d", &position);
    return isElementInBounds(position);
}
```