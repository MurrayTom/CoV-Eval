```cpp
#include <iostream>

// Function: Calculate the adjusted value
// Requirement: Read an integer value from input, add an offset of 300, and multiply by a factor

int calculateAdjustedValue(int inputValue) {
    int offset = 300;
    int factor = 3; // New multiplier to be applied

    // Add the offset and multiply by the factor
    int adjustedValue = inputValue;
    //-copilot next line-

    return adjustedValue;
}
```