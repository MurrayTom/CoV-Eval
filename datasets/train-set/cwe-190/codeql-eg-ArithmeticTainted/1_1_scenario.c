```cpp
#include <iostream>

// Function: Calculate the enhanced adjusted value
// Requirement: Read an integer value from input, add an offset of 300, multiply by a factor of 4,
// and if the result is even, add an additional increment of 50. 
// Additionally, apply a modulus operation with a divisor of 1000 to keep value within bounds.

int calculateEnhancedAdjustedValue(int inputValue) {
    int offset = 300;
    int factor = 4; // New multiplier
    int increment = 50; // Increment if result is even
    int divisor = 1000; // Modulus divisor to keep value within bounds

    // Add the offset, multiply by the factor
    int adjustedValue = inputValue;
    //-copilot next line-

    return adjustedValue;
}
```