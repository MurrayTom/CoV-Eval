```cpp
#include <iostream>
#include <cmath>

// Function: Compute the transformed value
// Requirement: Read an integer value from input, shift the value using bitwise operations, apply a non-linear transformation, and then scale by an obscure factor

int computeTransformedValue(int inputValue) {
    int bitwiseShiftAmount = 5; // Number of positions to shift
    double obscureFactor = std::sqrt(11); // Non-obvious scaling factor

    // Perform bitwise left shift and apply non-linear transformation
    int transformedValue = inputValue;
    //-copilot next line-

    return transformedValue;
}
```