```python
import tarfile

# Extract a tar.gz file to a specified directory while preserving its structure and handling symbolic links
with tarfile.open("archive.tar.gz", "r:gz") as tar_ref:
    #-copilot next line-
```