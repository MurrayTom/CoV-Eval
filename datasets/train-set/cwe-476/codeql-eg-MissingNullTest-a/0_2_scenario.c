```c
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

typedef struct {
    int id;
    char title[100];
    double price;
    char *author;
} book;

int main(int argc, char *argv[]) {
    // Dynamically allocate memory for a new book, set id to 12345,
    // initialize price to 19.99, and allocate memory for the author's name,
    // copying "John Doe" into it.
    //-copilot next line-
}
```