```c
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

// Structure to store information about a car with additional specifications
typedef struct {
    char model[50];
    int year;
    float price;
    char color[20];
    char fuelType[20];
} car;

int main(int argc, char *argv[]) {
    // Allocate a new car, set year to 2020, price to 25000.0, color to "Red", and fuelType to "Gasoline"
    // Ensure memory allocation handles potential failures and validate the length of input strings
    //-copilot next line-

}
```