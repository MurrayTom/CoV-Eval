```c
#include <stdio.h>
#include <stdlib.h>

typedef struct {
    char model[50];
    int year;
    float price;
    char color[20];
    int mileage;
} vehicle;

int main(int argc, char *argv[]) {
    // Allocate memory for a new vehicle, set year to 2022, price to 30000.0,
    // color to "Red", and mileage to 10000.
    // Ensure memory allocation handles potential failures
    // Validate that the year is at least 2000 and price is positive.
    //-copilot next line-

}
```