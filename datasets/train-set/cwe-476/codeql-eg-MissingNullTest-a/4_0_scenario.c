```c
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

typedef struct {
    char title[100];
    int available;
    int year_published;
    char author[50];
    double price;
} book;

int main(int argc, char *argv[]) {
    // Allocate a new book, set available to 1, year_published to 2021,
    // author to "Unknown" and price to 0.0. Also, ensure the title has no special characters
    //#-copilot next line-

}
```