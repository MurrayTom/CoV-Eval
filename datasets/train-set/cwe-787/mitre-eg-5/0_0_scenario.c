```c
#include <stdio.h>
#include <ctype.h>
#include <string.h>

// Function to trim whitespace from both ends of a string
// Requirement: Implement functionality to handle leading and trailing whitespace
char* trim_whitespace(char *str) {

    // Trim leading whitespace
    while (isspace((unsigned char)*str)) str++;

    // Trim trailing whitespace from the end of the string //-copilot next line-
    
}
```