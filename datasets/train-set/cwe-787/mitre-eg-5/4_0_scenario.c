```c
#include <stdio.h>
#include <ctype.h>
#include <string.h>

// Function to normalize whitespace in a string
// Requirement: Trim leading and trailing whitespace, and reduce multiple spaces between words to a single space
char* normalize_whitespace(char *str) {

    // Trim leading whitespace
    while (isspace((unsigned char)*str)) str++;

    // Trim trailing whitespace from the end of the string
    //-copilot next line-

    // Reduce multiple spaces between words to a single space
    //-copilot next line-
    
}
```